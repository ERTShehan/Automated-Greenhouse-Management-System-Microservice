DB_USERNAME: root
DB_PASSWORD: Ijse@1234

spring:
  application:
    name: crop-service
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
  datasource:
    url: jdbc:mysql://localhost:3306/crop_db?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Ijse@1234}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8084